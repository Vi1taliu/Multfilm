#include "TXLib.h"

void drawHangar()
{
    txSetColor (RGB ( 0, 0, 0), 

    txSetFillColor (TX_LIGHTBLUE);
    txRectangle (0, 0, 800, 600);
}

int main()
    {
    txCreateWindow (800, 600);

    txTextCursor (false);
    return 0;
    }

